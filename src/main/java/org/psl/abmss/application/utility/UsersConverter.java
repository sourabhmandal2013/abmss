package org.psl.abmss.application.utility;

public class UsersConverter {
	
	/*@Autowired
	private RoleRepository roleRepository;
	
	@Autowired
	private UserRepository userRepository;
	*/
	
	/*public Users dtoToUsers(UserDTO usersDTO) {
		Users user = new Users();

		Users fromRepo 
		
		String buff[] = usersDTO.getAppliedRole().split(",");
		Set<Role> roles = new HashSet<Role>();   
		roleRepository.findAllByRole(buff).forEach(roles :: add);;
		
		user.setActive(usersDTO.isActive());
		user.setRoles(roles);
		user.setUserEmail(usersDTO.getUserEmail());
		user.setUserName(usersDTO.getUserName());
	}*/
}
